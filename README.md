# SMS analysis

In this project I load texts/imessages from my chat.db and analyse word frequencies and more. 

Set up the environment with `conda create -f [environment.yml](environment.yml)` then `conda activate generic-python`. 

